const BaseUrl = "http://localhost:5000"; // or your backend URL

const API = {
  staff: `${BaseUrl}/staff`,
//   users: `${BaseUrl}/users`,
//   issues: `${BaseUrl}/issues`,
    courses:`${BaseUrl}/course`,
    programs:`${BaseUrl}/program`,
    timetables:`${BaseUrl}/timetable`,
    students:`${BaseUrl}/student`,
};

export default API;
